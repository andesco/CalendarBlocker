{
  "name": "blocker",
  "version": "0.1.0",
  "description": "CalendarBlocker generates deterministic .ics calendar feeds for quickly blocking unavailable time.",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev -c wrangler.jsonc",
    "deploy": "wrangler deploy -c wrangler.jsonc",
    "dev:local": "wrangler dev -c wrangler.local.jsonc",
    "deploy:local": "wrangler deploy -c wrangler.local.jsonc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.7.2"
  },
  "dependencies": {
    "marked": "^15.0.11"
  }
}
